> **Estado de la verificación.** Cada tarea marcada se ha verificado con lo más fuerte disponible
> sin navegador: sintaxis, `phpcs` con el estándar WordPress, pruebas unitarias de las funciones
> puras y, sobre todo, una instalación real de WordPress 7.1 + WooCommerce 11.1.0 (sobre SQLite)
> contra la que se han ejecutado las rutas REST con `rest_do_request()` por cada rol y se ha pintado
> la pantalla del escritorio. Lo que solo se puede comprobar en un navegador (atajos de teclado,
> foco, tema, diseño adaptable y lector de pantalla) está recogido, escenario a escenario, en
> `docs/qa.md`: 39 comprobaciones pendientes de 101.

## 1. Andamiaje del repositorio y del plugin

- [x] 1.1 Crear la estructura de la raíz (`CLAUDE.md` con arquitectura y convenciones, `README.md`, `CHANGELOG.md`, `BACKLOG.md`, `.gitignore`, `.editorconfig`) y verificar que ningún fichero de desarrollo queda dentro de `escritoriowp/`
- [x] 1.2 Crear `composer.json` (solo dev: phpcs, WPCS, PHPCompatibilityWP, wp-cli/i18n) y `phpcs.xml.dist` (WordPress + PHPCompatibilityWP, `testVersion 8.1-`, prefijo `escritoriowp`, dominio `escritoriowp`) y verificar que `composer validate` pasa cuando Composer esté disponible (si no, validar el JSON con `php -r 'json_decode(...)'`)
- [x] 1.3 Crear `escritoriowp/escritoriowp.php` con cabecera completa (`Requires at least: 6.7`, `Requires PHP: 8.1`, `Text Domain`, `Version`, `WC tested up to`), constantes `ESCRITORIOWP_*`, comprobación de requisitos con aviso `admin_notices` y arranque en `plugins_loaded`; verificar con `php -l` y comprobar que con PHP/WP insuficientes no se cargan módulos (simular bajando la constante mínima)
- [x] 1.4 Implementar `includes/class-autoloader.php` (PSR-4 manual `EscritorioWP\` → `includes/`, prefijos `class-`/`interface-`, subcarpetas en minúsculas) y `includes/class-plugin.php` (singleton que registra módulos) y verificar con un script PHP en `tests/` que resuelve rutas para tres nombres de clase de ejemplo
- [x] 1.5 Crear `escritoriowp/readme.txt` (descripción, FAQ, changelog 0.1.0), `uninstall.php` (borra opción, metas `escritoriowp_preferencias`, transitorios `escritoriowp_*`) y verificar `php -l` de ambos
- [x] 1.6 Crear `.github/workflows/release.yml` (lint PHP + zip de `escritoriowp/` como release al empujar tag `v*`) y verificar la sintaxis YAML con `node -e "require('yaml')"` o `python3 -c "import yaml"`

## 2. Núcleo: ajustes, capacidades, WooCommerce e i18n

- [x] 2.1 Implementar `Ajustes` (opción `escritoriowp_ajustes`, defaults de la spec, fusión con defaults al leer, saneado por clave con recorte 3–20 para elementos por panel) y verificar con un test PHP puro que el saneado de `{ 'elementos_por_panel': 50 }` devuelve 20 y de una opción vacía devuelve todos los defaults
- [x] 2.2 Implementar la página Ajustes › EscritorioWP (`add_options_page`, Settings API con nonce, `manage_options`, casillas y campo numérico, `assets/css/ajustes.css`) y verificar manualmente que guardar cambia la opción y que un Editor obtiene "No tienes permisos"
- [x] 2.3 Implementar `Capacidades` (mapa tipo → capacidad de ver/crear/publicar, `puede_ver( tipo )`, `puede_crear( tipo )`, URLs de listado y de "nuevo" por tipo) y verificar con un test PHP puro que el mapa cubre los siete tipos de la spec
- [x] 2.4 Implementar `Woo` (detección `class_exists( 'WooCommerce' )`, declaración HPOS en `before_woocommerce_init`, URL de nuevo pedido según `OrderUtil::custom_orders_table_usage_is_enabled()`, formato de precio) y verificar que sin WooCommerce ninguna llamada a funciones `wc_*` se ejecuta (grep de guardas `if ( Woo::activo() )`)
- [x] 2.5 Configurar i18n (`load_plugin_textdomain`, todos los textos por `__()`/`esc_html__()` con dominio `escritoriowp`, textos JS pasados en el bootstrap) y generar `languages/escritoriowp.pot` con `wp i18n make-pot` (o documentar el comando si no está disponible); verificar con grep que no hay cadenas literales en español dentro de los `.js`

## 3. Activos y bootstrap del frontend

- [x] 3.1 Implementar `Activos` (registro de `comun.css/js`, `escritorio.css/js`, `lanzador.css/js` con versión = constante o `filemtime`, y `wp_add_inline_script` con `window.escritoriowpConfig`: URLs REST, nonce, capacidades, textos, preferencias, plataforma) y verificar en el HTML del admin que el objeto se imprime antes del script
- [x] 3.2 Escribir `assets/css/comun.css` con tokens de diseño (paleta clara y oscura, radios, sombras, tipografía del sistema, etiquetas de estado, botones, skeleton, `prefers-reduced-motion`) y verificar el contraste AA de texto sobre fondo en ambos temas con una herramienta de contraste
- [x] 3.3 Escribir `assets/js/comun.js` (peticiones REST con nonce y `AbortController`, escape HTML, normalización sin acentos, detección macOS para ⌘/Ctrl, `debounce`, utilidades de foco y trampa de foco, mini-plantillas) y verificar con `node --check` más un test Node en `tests/` para `normalizar('Páginas') === 'paginas'` y el escape de `<script>`

## 4. Escritorio: sustitución, resumen y paneles

- [x] 4.1 Implementar `Escritorio` (hook `wp_dashboard_setup` en `PHP_INT_MAX` que quita metaboxes nativos y de WooCommerce, y los de terceros según ajuste; quita `wp_welcome_panel`; fuerza 1 columna y oculta opciones de pantalla; imprime `#escritoriowp-app` vía `do_meta_boxes` y la cabecera "Otros widgets" si quedan; clase de body) y verificar manualmente que el escritorio muestra la interfaz propia, que los `admin_notices` siguen visibles y que con el ajuste desactivado vuelve el escritorio nativo
- [x] 4.2 Renderizar en PHP la cabecera (saludo con `display_name`, fecha con `wp_date` e `date_format`, botones de creación según `Capacidades`, pista ⌘K/Ctrl+K por `User-Agent`) y verificar que un Editor no ve "Nuevo usuario" ni botones de WooCommerce sin WooCommerce
- [x] 4.3 Implementar `Resumen` (KPIs con `wp_count_posts`, `count_users`, `wp_count_comments`, `wp_get_update_data`; con WooCommerce productos publicados, pedidos e ingresos del mes por `wc_get_orders` en estados `completed`/`processing`, transitorio de 5 min y bypass con `refrescar`) y verificar con un test PHP puro que el cálculo de ingresos a partir de una lista de pedidos simulados excluye cancelados y reembolsados
- [x] 4.4 Implementar `Recientes` (últimos N por tipo: entradas y páginas por `post_modified` con estado, autor y URLs; usuarios con avatar, rol y registro; productos con imagen, precio, stock y estado; pedidos con número, cliente, total, estado y fecha; URL de vista solo si publicado) y verificar manualmente que cada panel muestra los campos de la spec y que N sigue el ajuste
- [x] 4.5 Implementar `Preferencias` (meta `escritoriowp_preferencias` con `paneles_ocultos`, `orden_paneles`, `tema`; saneado de valores; defaults) y verificar con un test PHP puro que un tema inválido cae en `auto` y que claves de panel desconocidas se descartan
- [x] 4.6 Implementar los controladores REST `/resumen`, `/recientes/{tipo}` y `/preferencias` con `permission_callback` por capacidad y verificar con `curl` autenticado (o desde el navegador) que un Autor recibe 403 en `/recientes/usuarios` y 200 en `/recientes/entradas`
- [x] 4.7 Escribir `assets/js/escritorio.js` (carga paralela de resumen y paneles con skeleton, estado de error con "Reintentar", estado vacío con "Crear el primero", acción "Ver" en pestaña nueva, botón "Actualizar", menú "Personalizar" con mostrar/ocultar, subir/bajar, tema y "Restablecer" persistidos por REST, aplicación de `data-tema`) y verificar manualmente los escenarios de la spec `escritorio-panel` incluidos ocultar un panel y comprobarlo en otro navegador
- [x] 4.8 Escribir `assets/css/escritorio.css` (rejilla de tarjetas KPI y paneles, apilado a una columna por debajo de 782 px, sin desplazamiento horizontal a 360 px, ancho completo del contenedor `postbox-container-1`, ocultación del chrome de metabox) y verificar en el navegador a 400 px y a 1600 px de ancho y con Tab por toda la interfaz

## 5. Creación rápida

- [x] 5.1 Implementar `Creador` y `CreadorEntrada`/`CreadorPagina` (validación de título, estado permitido según `publish_*`, categoría opcional, `wp_insert_post`, respuesta con URLs de edición y vista) y verificar con `curl` que un título vacío devuelve 400 con `campos.titulo` y que un Autor recibe 403 al publicar
- [x] 5.2 Implementar `CreadorUsuario` (validación de login y email únicos con `username_exists`/`email_exists`, rol dentro de `get_editable_roles`, contraseña generada, `wp_new_user_notification` opcional) y verificar con `curl` que un email duplicado devuelve 400 con `campos.email` y que un rol no editable devuelve 403
- [x] 5.3 Implementar `CreadorProducto` (`WC_Product_Simple`, precio numérico no negativo, SKU único con `wc_product_has_unique_sku`, estado) y verificar creando un producto en borrador y comprobando precio y SKU en su ficha
- [x] 5.4 Registrar la ruta REST `/crear/{tipo}` con permisos por tipo y verificar que sin WooCommerce `/crear/producto` devuelve 404 o 403
- [x] 5.5 Implementar el modal de creación en `escritorio.js` (formularios por tipo, roles y categorías del bootstrap, errores por campo sin cerrar, confirmación con "Editar", "Ver", "Crear otro", "Cerrar", recarga del panel afectado, cierre con Escape y devolución del foco) y verificar manualmente los cuatro escenarios de creación de la spec `creacion-rapida`
- [x] 5.6 Enlazar "Nuevo pedido" a la URL nativa devuelta por `Woo` y verificar que abre la pantalla correcta con HPOS activado y desactivado

## 6. Búsqueda unificada (servidor)

- [x] 6.1 Definir `Fuente` (interfaz), `Resultado` (DTO con id, tipo, título, subtítulo, url_editar, url_ver, icono, fecha) y `Buscador` (filtra fuentes por solicitud, disponibilidad y `puede_buscar`, orden fijo de grupos, total y URL de listado nativo por grupo) y verificar con un test PHP puro con fuentes falsas que el orden de grupos y los límites se respetan
- [x] 6.2 Implementar `FuenteContenidos` (instancias entradas, páginas y otros; `WP_Query` con filtros `posts_where`/`posts_orderby` propios para título-solo, prioridad de prefijo y restricción de borradores ajenos sin `edit_others_posts`; exclusión de tipos internos) y verificar manualmente los escenarios "coincidencia solo por título", "colaborador y borradores ajenos" y "orden de resultados"
- [x] 6.3 Implementar `FuenteUsuarios` (`WP_User_Query` con `search_columns` explícitas, `count_total`, subtítulo email + rol, solo con `list_users`) y verificar buscando "@" + dominio y que un Editor sin `list_users` no recibe el grupo
- [x] 6.4 Implementar `FuenteProductos` (`search_products` con variaciones → padre, deduplicación, hidratación con `wc_get_product`, subtítulo precio + stock + estado) y verificar buscando un SKU de variación y obteniendo el producto padre
- [x] 6.5 Implementar `FuentePedidos` (número exacto con o sin `#` vía `wc_get_order`, `wc_order_search` → `wc_get_orders` ordenados por fecha, subtítulo cliente + total + estado) y verificar buscando "#<número>" y un fragmento de email de facturación con HPOS activado y desactivado
- [x] 6.6 Implementar el controlador REST `/buscar` (validación de `q` 2–100 caracteres, `tipos[]`, `limite` 1–20, 401 sin sesión, 400 por texto corto) y verificar con `curl` los tres códigos de respuesta y la estructura de grupos de la spec

## 7. Lanzador (cliente y desactivación de la paleta nativa)

- [x] 7.1 Implementar `Lanzador` (hook `admin_enqueue_scripts` prioridad 1: detectar editor de bloques y `site-editor.php`, `remove_action` de `wp_enqueue_command_palette_assets` según ajuste, encolado del lanzador en admin y en front-end con barra visible según ajuste) y verificar en WordPress 6.9+ que Cmd/Ctrl+K en Entradas abre el lanzador propio y dentro del editor de bloques abre la paleta nativa
- [x] 7.2 Añadir el botón "Buscar ⌘K/Ctrl+K" a la barra de administración (`admin_bar_menu` prioridad 56, admin y front-end) y verificar que aparece en ambos contextos y que el botón nativo de 7.0 ya no aparece cuando la paleta nativa está desactivada
- [x] 7.3 Implementar `CatalogoComandos` (recorrido de `$menu`/`$submenu` con comprobación de capacidad, limpieza de títulos, resolución de URLs, ruta "Padre › Hijo", icono; acciones fijas condicionadas; transitorio por usuario 12 h para el front-end) y verificar con un test PHP puro de resolución de URLs (`edit.php`, `admin.php?page=x`, submenú de `options-general.php`) y manualmente que "WooCommerce › Ajustes" aparece
- [x] 7.4 Escribir `assets/js/lanzador.js` — apertura/cierre (atajo en fase de captura con `preventDefault`, botón de la barra, Escape y clic fuera, devolución de foco), estado inicial con recientes en `localStorage` y acciones rápidas, y verificar manualmente que Ctrl+K con el foco en un `<input>` abre el lanzador sin que el navegador enfoque su barra de direcciones
- [x] 7.5 Implementar en `lanzador.js` la coincidencia de comandos en cliente (normalización sin acentos, puntuación por prefijo de palabra, subcadena y todas las palabras) y verificar con un test Node en `tests/` que "pagina" coincide con "Páginas" y "plug" ordena "Plugins" antes que "Plugins › Añadir nuevo"
- [x] 7.6 Implementar en `lanzador.js` la búsqueda de contenido (a partir de 2 caracteres, debounce 200 ms, `AbortController` para peticiones obsoletas, render agrupado en el orden de la spec, "Ver todos en el listado", "Sin resultados", aviso de error no bloqueante) y verificar manualmente los escenarios "resultado mixto", "peticiones obsoletas" y "error del servidor" (simulando un 500)
- [x] 7.7 Implementar filtros por tipo (chips según capacidades y WooCommerce, rotación con Tab/Shift+Tab) y navegación por teclado (flechas saltando cabeceras con auto-scroll, Enter, Cmd/Ctrl+Enter en pestaña nueva, Shift+Enter acción secundaria, selección inicial en el primer resultado, ratón) y verificar manualmente cada atajo de la spec `lanzador-comandos`
- [x] 7.8 Escribir `assets/css/lanzador.css` (diálogo centrado con fondo difuminado, campo de búsqueda grande, cabeceras de grupo, fila seleccionada, chips, atajos `<kbd>`, tema claro/oscuro compartido con el escritorio, adaptación a 360 px) y aplicar accesibilidad (`role="dialog"`, `aria-modal`, `aria-labelledby`, `role="listbox"`/`option`, `aria-activedescendant`, trampa de foco, `aria-live` para el número de resultados) verificándolo con VoiceOver o el inspector de accesibilidad del navegador

## 8. Verificación integral y entrega

- [x] 8.1 Ejecutar `php -l` sobre todos los `.php` y `node --check` sobre todos los `.js`, y los tests de `tests/` con `php` y `node`, verificando que todo pasa sin errores ni avisos
- [x] 8.2 Ejecutar `phpcs` con `phpcs.xml.dist` (en CI o localmente si Composer está disponible) y verificar cero errores
- [ ] 8.3 Redactar `docs/qa.md` con la lista de comprobación manual derivada de todos los escenarios de las cinco specs (WordPress 7.x, con y sin WooCommerce, HPOS activado y desactivado, roles Administrador, Editor, Autor, Colaborador y Gestor de tienda) y ejecutarla en una instalación local o staging, anotando resultado por escenario — **documento redactado y 62 de 101 escenarios ya verificados en servidor; quedan 39 que exigen navegador, más el camino con HPOS activo, que el entorno de pruebas con SQLite no puede levantar**
- [x] 8.4 Verificar la desinstalación: borrar el plugin desde Plugins y comprobar en la base de datos que no queda ninguna fila con prefijo `escritoriowp` en `wp_options`, `wp_usermeta` ni transitorios
- [x] 8.5 Completar `CLAUDE.md` (estructura, convenciones, cómo añadir una fuente de búsqueda o un panel, proceso de release), `readme.txt` (changelog 0.1.0) y `CHANGELOG.md`, fijar versión 0.1.0 en cabecera, constante y `Stable tag`, y verificar que las tres coinciden con un grep
- [x] 8.6 Generar el zip de `escritoriowp/` (localmente con `zip -rq` o vía el workflow) e instalarlo en una WordPress limpia verificando que activa sin errores y sin ficheros de desarrollo dentro
