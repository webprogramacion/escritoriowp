## 1. Herramientas de empaquetado y notas de release

- [x] 1.1 Crear `tools/notas-release.php` con una función pura `extraer_seccion_changelog( string $markdown, string $version ): ?string` que devuelva la sección `## [X.Y.Z]` de `CHANGELOG.md` hasta la siguiente cabecera `## `, y un punto de entrada CLI que la imprima o termine con código 1 si no existe; verificar con `tests/prueba-notas-release.php` (sección existente, inexistente, última del fichero) y ejecutando `php tools/notas-release.php 0.1.0`
- [x] 1.2 Crear `tools/empaquetar.php` que genere `dist/escritoriowp-X.Y.Z.zip` a partir de `escritoriowp/` con la versión leída de la cabecera, excluya `.DS_Store`, `Thumbs.db` y `*.map`, y falle si la única entrada raíz del zip no es `escritoriowp/`; añadir `dist/` a `.gitignore`; verificar ejecutándolo y listando el zip con `unzip -l` (solo `escritoriowp/…`, sin `.DS_Store`)
- [x] 1.3 Añadir a `tools/empaquetar.php` la opción `--wordpress-org`: omite `includes/actualizaciones/`, quita la línea `Update URI` y deja vacía `ESCRITORIOWP_REPOSITORIO` en la copia empaquetada, sin tocar el árbol de trabajo; extraer la transformación de la cabecera a una función pura y verificar con `tests/prueba-empaquetar.php` (la cabecera sale sin `Update URI`, la constante vacía, el resto intacto) y con `unzip -l` del zip resultante (no aparece `includes/actualizaciones/`)
- [x] 1.4 Registrar los ficheros nuevos de `tools/` en `README.md` (sección «Comandos útiles») y añadir las pruebas nuevas al arranque de `tests/ejecutar.php`; verificar que `php tests/ejecutar.php` pasa

## 2. Workflow de GitHub Actions

- [x] 2.1 Reescribir `.github/workflows/release.yml`: disparo en `push` a `main`, `pull_request` a `main` y `workflow_dispatch`; `concurrency` con grupo `release-main`; job `version` que lee la cabecera, hace `git fetch --tags` y expone las salidas `version` y `publicar` según exista `vX.Y.Z`; verificar la sintaxis YAML con `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/release.yml'))"`
- [x] 2.2 Job `comprobar` (siempre): sintaxis PHP y JS, `composer lint`, `php tests/ejecutar.php`, `node tests/ejecutar.js`, `node tools/comprobar-contraste.js` y el paso `wordpress/plugin-check-action@v1` sobre `escritoriowp/` con `ignore-warnings: false` y `ignore-codes` vacío (cada código que se añada lleva comentario); verificar en la primera ejecución sobre `main` que el job termina en verde
- [x] 2.3 Job `publicar` (solo `push` y `publicar == 'true'`, `needs: [version, comprobar]`): comprueba que cabecera, constante `VERSION` y `Stable tag` coinciden y que `readme.txt` contiene `= X.Y.Z =`; genera las notas con `php tools/notas-release.php` (falla si no hay sección); empaqueta con `php tools/empaquetar.php`; publica con `softprops/action-gh-release@v2` (`tag_name: vX.Y.Z`, `target_commitish: ${{ github.sha }}`, `name: X.Y.Z`, `body_path`, `files: dist/escritoriowp-*.zip`, `make_latest: true`); verificar con la publicación real de 0.2.0 (tarea 7.3)
- [x] 2.4 Actualizar la sección «Publicar una versión» de `CLAUDE.md` y `README.md`: ya no se empujan tags a mano; el push a `main` con la versión subida y el changelog escrito publica solo; verificar releyendo que no queda ninguna referencia a «empuja el tag»

## 3. Actualizador desde GitHub

- [x] 3.1 Añadir a `escritoriowp/escritoriowp.php` la cabecera `Update URI: https://github.com/webprogramacion/escritoriowp` y la constante `ESCRITORIOWP_REPOSITORIO` con la misma URL; verificar con `php -l` y con `get_plugin_data()` en el entorno local que `UpdateURI` se lee
- [x] 3.2 Crear `includes/actualizaciones/class-actualizador-github.php` (`EscritorioWP\Actualizaciones\ActualizadorGithub`) con la función pura estática `interpretar_release( array $json ): ?array` (quita la `v` del tag, valida la versión, elige el primer activo `.zip`, devuelve `version`, `paquete`, `url`, `notas`, `publicada`, o `null`); verificar con `tests/prueba-actualizador.php` (release válida, tag sin `v`, sin activos zip, tag no numérico, activo `.zip` en segunda posición) añadido a `tests/ejecutar.php`
- [x] 3.3 Implementar en la misma clase la obtención con caché: `wp_remote_get()` a `releases/latest` con `Accept`, `User-Agent` y timeout 10 s; transitorio `escritoriowp_actualizacion` 12 h con el resultado y 1 h con `array( 'error' => true )` ante fallo; verificar en el entorno local con el filtro `pre_http_request` devolviendo un JSON de ejemplo que solo se hace una petición en dos llamadas seguidas y que un error deja el transitorio de 1 h
- [x] 3.4 Enganchar `update_plugins_github.com` devolviendo el array de actualización solo para `ESCRITORIOWP_BASENAME` y `false` sin datos; verificar en el entorno local que, con el JSON simulado en `0.9.0`, `wp_update_plugins()` deja al plugin en `response` del transitorio `update_plugins`, y con `0.0.1` lo deja en `no_update`
- [x] 3.5 Enganchar `plugins_api` para `plugin_information` con slug `escritoriowp` devolviendo nombre, versión, autor, requisitos, `last_updated`, `download_link` y `sections` (descripción y changelog con `wp_kses_post( wpautop() )`); verificar en el entorno local llamando a `plugins_api( 'plugin_information', array( 'slug' => 'escritoriowp' ) )` y comprobando que no llega a WordPress.org (filtro `pre_http_request` que falla si se llama a `api.wordpress.org`)
- [x] 3.6 Registrar `admin_post_escritoriowp_buscar_actualizaciones`: exige `update_plugins` y nonce, borra el transitorio, ejecuta `wp_clean_plugins_cache( true )` y `wp_update_plugins()`, redirige a Acerca de con `comprobado=1`; verificar en el entorno local que sin nonce termina con `wp_die` y con nonce redirige y ha borrado la caché
- [x] 3.7 Instanciar el actualizador desde `Plugin::arrancar()` solo si `ESCRITORIOWP_REPOSITORIO` no está vacía; verificar que con la constante vacía `has_filter( 'update_plugins_github.com' )` devuelve `false`

## 4. Menú propio: Ajustes y Acerca de

- [x] 4.1 Cambiar `Ajustes::anadir_pagina()` a `add_menu_page()` + dos `add_submenu_page()` («Ajustes» con slug `escritoriowp`, «Acerca de» con slug `escritoriowp-acerca`, ambos `manage_options`, icono `dashicons-dashboard`), guardar los hooks devueltos y usarlos en `encolar()`; añadir `Ajustes::url()`; verificar en el entorno local que `$submenu['escritoriowp']` tiene las dos entradas en ese orden y que `ajustes.css` se encola en ambas pantallas
- [x] 4.2 Sustituir en `CatalogoComandos` la URL `options-general.php?page=…` por `Ajustes::url()`; verificar con `tests/prueba-catalogo.php` (o ampliándola) que el comando de ajustes apunta a `admin.php?page=escritoriowp`
- [x] 4.3 Crear `includes/class-changelog.php` con `Changelog::desde_readme( string $contenido ): array` (localiza `== Changelog ==`, corta en la siguiente `== … ==`, devuelve versiones con sus puntos en el orden del fichero); verificar con `tests/prueba-changelog.php` (dos versiones, sin sección, sección al final del fichero, líneas que no empiezan por `* ` ignoradas) añadido a `tests/ejecutar.php`
- [x] 4.4 Crear `includes/class-acerca-de.php` (`EscritorioWP\AcercaDe`): cabecera con nombre y versión instalada, descripción, enlaces al repositorio, releases e `Author URI` con `target="_blank" rel="noopener"`, bloque «Novedades» desde `Changelog` marcando la versión instalada y texto alternativo si no hay historial; todo escapado; verificar pintándola en el entorno local con `do_action( $hook )` y comprobando que el HTML contiene `0.1.0`, los enlaces y la lista, y que con un `readme.txt` sin changelog aparece el texto alternativo sin avisos de PHP
- [x] 4.5 Añadir a `AcercaDe` el bloque de estado de actualización (versión conocida o «al día», botón «Buscar actualizaciones ahora» en formulario a `admin-post.php` con nonce, enlace a Plugins cuando hay versión nueva, aviso tras `comprobado=1`), solo si recibe un actualizador y el usuario tiene `update_plugins`; verificar en el entorno local con el transitorio simulado en `0.9.0` (aparece el enlace) y con el actualizador nulo (no aparece el bloque)
- [x] 4.6 Componer `AcercaDe` en `Plugin::arrancar()`, ampliar `assets/css/ajustes.css` con los estilos de Acerca de (tokens de `comun.css`, tema oscuro incluido) y añadir los textos nuevos; verificar con `node tools/comprobar-contraste.js` y `composer lint`
- [x] 4.7 Sustituir «Ajustes › EscritorioWP» por «EscritorioWP › Ajustes» en `readme.txt`, `README.md`, `CLAUDE.md`, `docs/qa.md` y los textos del plugin; verificar con `grep -rn "Ajustes › EscritorioWP\|options-general.php?page=escritoriowp"` que no queda ninguna aparición

## 5. Cumplimiento de WordPress.org

- [x] 5.1 Añadir a `readme.txt` la sección de servicios externos (API de GitHub: para qué, cuándo, que no se envían datos del sitio ni del usuario, enlaces a condiciones y privacidad de GitHub) y la explicación del menú propio y de Acerca de; verificar que la sección existe y que `readme.txt` mantiene `Stable tag`, `Tested up to` y `Requires PHP` válidos
- [x] 5.2 Ejecutar Plugin Check sobre `escritoriowp/` en el entorno local (plugin Plugin Check instalado y su clase de ejecución llamada desde un script PHP) y corregir todos los errores; para cada aviso que se decida tolerar, añadir su código a `ignore-codes` del workflow con un comentario; verificar que la ejecución local termina sin errores
- [x] 5.3 Ejecutar Plugin Check sobre el zip generado con `--wordpress-org` y verificar que también pasa sin errores y sin los códigos excluidos por el actualizador
- [x] 5.4 Redactar `docs/publicar-wordpress-org.md`: lista de comprobación (readme, capturas y recursos gráficos, cabeceras, licencia, `Tested up to`, variante sin actualizador con `tools/empaquetar.php --wordpress-org`, envío del zip, SVN y etiquetado); enlazarla desde `README.md` y `CLAUDE.md`; verificar que cubre todos los puntos del requisito «Guía de publicación» de la spec

## 6. Pruebas, documentación y versión

- [x] 6.1 Actualizar `CLAUDE.md`: estructura (`class-acerca-de.php`, `class-changelog.php`, `actualizaciones/`, `tools/` nuevos, `docs/publicar-wordpress-org.md`), caché nueva `escritoriowp_actualizacion` en la tabla de cachés, sección de menú propio y de actualizador con la nota sobre la directriz 8; verificar releyendo que la estructura coincide con `find escritoriowp -type f`
- [x] 6.2 Ampliar `docs/qa.md` con los escenarios de las cuatro specs nuevas y el modificado (menú visible solo para `manage_options`, Acerca de, buscar actualizaciones, aviso en Plugins, «Ver detalles», «Actualizar ahora», instalación manual del zip); verificar que cada requisito de las specs tiene al menos una línea
- [x] 6.3 Subir la versión a `0.2.0` en cabecera, constante `VERSION` y `Stable tag`; escribir la entrada `## [0.2.0]` en `CHANGELOG.md` y `= 0.2.0 =` en `readme.txt` (con `Upgrade Notice`); regenerar el `.pot` con `php tools/generar-pot.php`; verificar que `php tools/notas-release.php 0.2.0` imprime la sección y que `grep` de las tres versiones devuelve `0.2.0`
- [x] 6.4 Pasar la batería completa en local: `composer test`, `composer lint`, `find escritoriowp -name '*.php' -print0 | xargs -0 -n1 php -l`, `node --check` de los JS y `php tools/empaquetar.php`; verificar que todo termina sin errores ni avisos

## 7. Verificación de extremo a extremo

- [x] 7.1 En el entorno local con SQLite: instalar el zip de `dist/` sobre una copia con `0.1.0` activa mediante `Plugin_Upgrader` y verificar que la versión pasa a `0.2.0`, el plugin sigue activo y `get_option( 'escritoriowp_ajustes' )` no cambia
- [x] 7.2 Archivar el cambio `create-escritoriowp-plugin` (`/opsx:archive`) para que exista la spec principal `nucleo-plugin` y verificar que `openspec validate releases-github-y-menu-propio` ya no avisa de que falta la spec objetivo
- [x] 7.3 Fusionar en `main` y verificar en GitHub que el workflow crea el tag `v0.2.0`, la release `0.2.0` marcada como última con las notas del changelog y el activo `escritoriowp-0.2.0.zip`; descargarlo y comprobar con `unzip -l` la estructura
- [ ] 7.4 En una instalación real con `0.2.0` instalada desde ese zip, apuntar temporalmente la constante a una release de prueba mayor (o publicar `0.2.1` cuando toque) y verificar el ciclo completo: aviso en Plugins, «Ver detalles» con el changelog, «Actualizar ahora» descarga de GitHub y deja el plugin activo; anotar el resultado en `docs/qa.md`
